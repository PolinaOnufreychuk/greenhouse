/**
 * Pill-shaped call-to-action button. Use `primary` for the single most
 * important action per section (Buy Tickets); `secondary`/`ghost` for
 * supporting actions.
 *
 * @example
 * <Button variant="primary" size="lg">Get Tickets</Button>
 */
