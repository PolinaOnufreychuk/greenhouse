/**
 * Oversized number + label stat, used in rows of 3–4 to convey scale
 * ("60+ species", "500 butterflies") without a data-heavy dashboard feel.
 *
 * @example
 * <StatBlock value="60+" label="Butterfly species" />
 */
