/**
 * Pricing tier card for the tickets section — one `featured` tier per
 * row, lifted and filled with the primary orange.
 *
 * @example
 * <TicketCard name="Family Pass" price="$68" unit="up to 4" perks={["Skip the line","Free return visit"]} featured />
 */
